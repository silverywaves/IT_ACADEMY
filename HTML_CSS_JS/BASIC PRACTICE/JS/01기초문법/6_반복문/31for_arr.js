// for 문은 횟수를 지정하는 반복문이라 배열과 같은 자료형을 반복횟수용도로 사용

let arr = ["banana", "apple", "orange"];
for (let i = 0; i < arr.length; i++){
    // 배열에 length 속성을 사용하면 배열의 데이터 개수(배열의 길이) 확인 가능
    console.log(arr[i]);
}