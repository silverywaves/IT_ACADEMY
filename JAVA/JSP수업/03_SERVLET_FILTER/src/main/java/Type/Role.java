package Type;	// Type 패키지 내에 열거형(enum) 정의

public enum Role {	// Role이라는 이름의 열거형(enum) 선언 (열거형 : 여러 상수를 하나의 타입으로 정의)
	ROLE_USER,		// 0
	ROLE_MANAGER,	// 1
	ROLE_ADMIN		// 2
	
	// 보통 사용자의 권한을 체크하거나 관리할 때 사용
}
