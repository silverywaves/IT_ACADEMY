/* 
    for(초깃값; 조건식; 증감식){
        블록문      // 횟수를 지정해 지정한 횟수가 끝날 때까지 블록문을 반복 실행
    }
    초깃값 -> 조건식 -> 블록문(조건식이 참일 경우) -> 증감식 ->조건식 순서로 진행
    초깃값은 최초 1회만 실행
*/

for(let i = 0; i < 5; i++){
    console.log(i);
}